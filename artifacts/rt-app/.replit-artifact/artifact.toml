kind = "web"
previewPath = "/"
title = "RT Management App"
version = "1.0.0"
id = "artifacts/rt-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19401

[services.development]
run = "pnpm --filter @workspace/rt-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rt-app", "run", "build" ]
publicDir = "artifacts/rt-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19401"
BASE_PATH = "/"
